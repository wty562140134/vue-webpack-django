# 前后分离,前端模块化架构
```angular2
python 3.6.2
django 2.1.7
vue-cli 3.5.1
node v10.15.3
            
因为开发依赖包太.不利于提交github所以删除了所有依赖.使用前需要更新依赖.

# expressRun是express前端服务器.使用前先进入下面更新依赖
npm install
   
# crmpages是vue+node搭建的前端模块化页面.使用前也需要去下面更新依赖
npm install
 
```
